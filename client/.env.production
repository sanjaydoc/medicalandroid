# Production build env for Cloudflare Pages + the Android app.
# Baked into every `npm run build`, so a fresh container re-clone still builds a
# correctly-configured bundle. The Supabase publishable/anon key is FRONTEND-SAFE
# (public by design; data is protected by Row-Level Security) — safe to commit.
VITE_STATIC=true
VITE_SUPABASE_URL=https://jeusnqudtuajafpjodjg.supabase.co
VITE_SUPABASE_KEY=sb_publishable_wA1RROIgG_VcqYaZMP5xtg_rPEiJjYh
VITE_CHAT_ENDPOINT=https://medicalandroid-chat.dr-sanjayanbu.workers.dev
